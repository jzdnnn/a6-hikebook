# ===== DEVELOPMENT CONFIGURATION =====
NODE_ENV=development

# Server Configuration
PORT=3000

# Database Connection (SQLite for development)
DATABASE_URL="file:./prisma/dev.db"

# Session Secret (change in production!)
SESSION_SECRET="hikebook-secret-key-2025-change-this-in-production"

# JWT Secret (if using JWT authentication)
JWT_SECRET="hikebook-secret-jwt-key-2025-super-secure-change-this"

# Application URL
APP_URL=http://localhost:3000

# ===== PRODUCTION CONFIGURATION =====
# Uncomment and configure these when deploying to production

# NODE_ENV=production
# PORT=3000

# Database Connection (SQLite for production - or use PostgreSQL)
# DATABASE_URL="file:./prisma/production.db"

# Session Secret (MUST CHANGE - generate random string)
# SESSION_SECRET="your-very-long-random-secret-key-minimum-32-characters"

# JWT Secret (MUST CHANGE - generate random string)
# JWT_SECRET="your-very-long-random-jwt-secret-minimum-32-characters"

# Application URL (your domain)
# APP_URL=https://yourdomain.com

# Optional: PostgreSQL for production (recommended for scalability)
# DATABASE_URL="postgresql://username:password@your-db-host:5432/hikebook?sslmode=require"
